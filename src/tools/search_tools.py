# Tools for searching inside files and directories.
#
# Planned tools:
#   - GrepSearch(pattern, path) — find lines matching a regex
#   - FindFiles(pattern, path)  — find files whose names match a glob
