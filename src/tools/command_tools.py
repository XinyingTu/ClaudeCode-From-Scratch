# Tools for running shell commands in a controlled way.
#
# Planned tools:
#   - RunCommand(command, cwd)  — execute a shell command and return stdout/stderr
#
# Safety note: always validate and sandbox commands before running them.
